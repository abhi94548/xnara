name: xnara
description: Food Recognition Application

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  flutter:
    sdk: flutter
  fluttertoast: ^8.0.8
  hive: ^2.0.4
  image_cropper: ^1.4.1
  image_picker: ^0.8.3+2
  lint: ^1.6.0
  path_provider: ^2.0.2
  permission_handler: ^8.1.4+2
  provider: ^5.0.0


dev_dependencies:
  build_runner: ^2.1.1
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.0

flutter:

  uses-material-design: true

